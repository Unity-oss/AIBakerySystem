{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "gunicorn tastyz_project.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 4"
  }
}
